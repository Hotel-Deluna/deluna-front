import React from "react";
import AuthLoginForm from "../../components/auth/authLoginForm";

const UserLogin=()=>{
    return(
        <AuthLoginForm type={'use'} />
    );
}

export default UserLogin;